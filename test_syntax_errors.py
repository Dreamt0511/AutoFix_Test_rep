def calculate_sum(numbers):
    total = 0
    for num in numbers
        total += num
    return total

def find_max(numbers):
    if not numbers
        return None
    max_val = numbers[0]
    for num in numbers[1:]:
        if num > max_val:
            max_val = num
    return max_val

def print_results(results):
    for result in results:
        print(f"结果: {result"

if __name__ == "__main__":
    test_numbers = [1, 2, 3, 4, 5]
    print("总和:", calculate_sum(test_numbers))
    print("最大值:", find_max(test_numbers))

    test_results = [10, 20, 30]
    print_results(test_results)
