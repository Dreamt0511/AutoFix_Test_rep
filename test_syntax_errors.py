def process_data(items):
    result = []
    for item in items  # 缺少冒号
        if item > 0:
            result.append(item * 2)
        else  # 缺少冒号
            result.append(item)
    return result

